services:
  mysql:
    image: mysql:8
    container_name: tele-sso-mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: tele_sso
    ports:
      - "3309:3306"
    restart: unless-stopped
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:

# Semua cron job dikelola oleh node-cron di dalam proses backend (src/scheduler.ts).
# Backend dan cloudflared tunnel dikelola sebagai systemd user services:
#
#   ~/.config/systemd/user/wknd-tele-bot.service
#   ~/.config/systemd/user/cloudflared-wknd.service
#
# Tidak ada crontab yang perlu dipasang.

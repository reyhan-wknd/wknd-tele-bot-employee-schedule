# Semua cron job dikelola oleh node-cron di dalam proses backend (src/scheduler.ts),
# dengan timezone Asia/Jakarta. Tidak ada crontab yang perlu dipasang.
#
# Produksi: backend dan cloudflared jalan sebagai container dalam satu compose stack
# (docker-compose.prod.yml), restart: unless-stopped. Lihat bagian "Deploy Produksi"
# di README.
#
# Unit systemd di backend/deploy/wknd-tele-bot.service adalah jalur alternatif
# non-Docker, bukan yang dipakai produksi saat ini.
#
# File ini sengaja dipertahankan supaya siapa pun yang mencarinya menemukan jawabannya
# di sini, bukan menyimpulkan cron-nya hilang.
